name: Tech News Digest Scheduler

on:
  # Otomatik zamanlanmış görevler (İstanbul saati)
  schedule:
    - cron: '35 1 * * *'  # Her gün saat 01:35 - RSS Crawl (test için)
    - cron: '36 1 * * *'  # Her gün saat 01:36 - Digest Send (test için)
  
  # Manuel tetikleme
  workflow_dispatch:
    inputs:
      job_type:
        description: 'Hangi job çalıştırılsın?'
        required: true
        default: 'crawl'
        type: choice
        options:
        - crawl
        - digest
        - both
      test_time:
        description: 'Test zamanı (opsiyonel)'
        required: false
        type: string
        default: ''

jobs:
  scheduler:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        cd backend
        pip install -r requirements.txt
    
    - name: Run RSS Crawl
      if: github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'crawl' || github.event.inputs.job_type == 'both'))
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
        SMTP_PORT: ${{ secrets.SMTP_PORT }}
        SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
        SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
        FROM_NAME: ${{ secrets.FROM_NAME }}
        DATABASE_TYPE: postgresql
        DAILY_CRAWL_HOUR: 1
        DAILY_CRAWL_MINUTE: 35
        DAILY_DIGEST_HOUR: 1
        DAILY_DIGEST_MINUTE: 36
      run: |
        cd backend
        python test_scheduler.py crawl
    
    - name: Run Digest Send
      if: github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'digest' || github.event.inputs.job_type == 'both'))
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
        SMTP_PORT: ${{ secrets.SMTP_PORT }}
        SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
        SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
        FROM_NAME: ${{ secrets.FROM_NAME }}
        DATABASE_TYPE: postgresql
        DAILY_CRAWL_HOUR: 1
        DAILY_CRAWL_MINUTE: 35
        DAILY_DIGEST_HOUR: 1
        DAILY_DIGEST_MINUTE: 36
      run: |
        cd backend
        python test_scheduler.py digest
    
    - name: Run Both Jobs
      if: github.event_name == 'workflow_dispatch' && github.event.inputs.job_type == 'both'
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
        SMTP_PORT: ${{ secrets.SMTP_PORT }}
        SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
        SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
        FROM_NAME: ${{ secrets.FROM_NAME }}
        DATABASE_TYPE: postgresql
        DAILY_CRAWL_HOUR: 1
        DAILY_CRAWL_MINUTE: 35
        DAILY_DIGEST_HOUR: 1
        DAILY_DIGEST_MINUTE: 36
      run: |
        cd backend
        python test_scheduler.py both
    
    - name: Test Results
      if: always()
      run: |
        echo "🎯 Test tamamlandı!"
        echo "📊 Job Type: ${{ github.event.inputs.job_type || 'scheduled' }}"
        echo "🕐 Test Zamanı: $(date)"
        echo "🌍 Timezone: Europe/Istanbul" 